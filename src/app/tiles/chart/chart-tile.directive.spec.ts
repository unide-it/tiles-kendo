import { ChartTileDirective } from './chart-tile.directive';

describe('ChartTileDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartTileDirective();
    expect(directive).toBeTruthy();
  });
});
